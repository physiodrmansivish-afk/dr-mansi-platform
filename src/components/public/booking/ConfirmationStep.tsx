// This component is no longer used in the simplified booking flow.
// Kept as a placeholder to avoid import errors from any residual references.
export default function ConfirmationStep() {
  return null;
}
